using UnityEngine;
using System.Collections;

public class TerrainGeneration : MonoBehaviour {
	public int MAX_CHUNKS_X = 5;
	public int MAX_CHUNKS_Y = 5;
	public int CHUNK_SIZE
	private Terrain[][] chunkMap;

	// Use this for initialization
	void Start () {
		//chunkMap = new Terrain[MAX_CHUNKS_X] [MAX_CHUNKS_Y];

	
	}
	
	// Update is called once per frame. Future use for loading in new area maybe?
	//void Update () {
	//
	//}
}
